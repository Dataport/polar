import { Meta, Controls, Primary, Canvas } from "@storybook/blocks";
import * as DpsLoadingSpinnerStories from "./DpsLoadingSpinner.stories";

<Meta of={DpsLoadingSpinnerStories} />

# DpsLoadingSpinner

## Description

The `DpsLoadingSpinner` component is a visual indicator used to display loading states. The component includes a slot, allowing to add a label, for example.

<Primary name="Default" />

## Properties, Events, Slots

<Controls />
