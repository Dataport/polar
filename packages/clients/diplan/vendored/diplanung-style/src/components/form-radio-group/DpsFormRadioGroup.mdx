import { Canvas, Meta, Controls, Primary } from "@storybook/blocks";
import * as DpsFormRadioGroupStories from "./DpsFormRadioGroup.stories";

<Meta of={DpsFormRadioGroupStories} />

# DpsFormRadioGroup

## Description

The `DpsFormRadioGroup` component can be used to display a group of `DpsFormRadio` components.

To configure the options, add them to the `options` property:

```json
[
  { "label": "Option 1", "value": "opt-1" },
  { "label": "Option 2", "value": "opt-2" },
  { "label": "Option 3", "value": "opt-3" }
]
```

<Primary name="Default" />

## Stories

### HTML Labels

You can also use HTML code in option labels.

<Canvas of={DpsFormRadioGroupStories.HtmlLabels} />

### Disabled Group

To disable the entire group, set the `disabled` property to `true`.

<Canvas of={DpsFormRadioGroupStories.DisabledGroup} />

### Disabled Option

To disable individual radios, add `disabled: true` to the objects within the array set in the
`options` property.

<Canvas of={DpsFormRadioGroupStories.DisabledOption} />

### Readonly Option

To render individual radios as read-only, add `readonly: true` to the objects within the array set
in the `options` property.

<Canvas of={DpsFormRadioGroupStories.ReadonlyOption} />

### Error Option

To render individual radios in their error state, add `error: true` to the objects within the array
set in the `options` property.

<Canvas of={DpsFormRadioGroupStories.ErrorOption} />

### Truncated Labels

To truncate the labels, set the `truncate` property to `true`.

<Canvas of={DpsFormRadioGroupStories.TruncatedLabels} />

### Object Values

This example demonstrates how to change the object keys for **value** and **label** using the
`optionLabel` and `optionValue` properties. In the provided options array, each object contains
multiple values, but only `value1` is used as the label and `value2` as the value.

<Canvas of={DpsFormRadioGroupStories.ObjectValues} />

### Inline

To display the radios next to each other, set the `inline` property to `true`.

<Canvas of={DpsFormRadioGroupStories.Inline} />

## Properties, Events, Slots

<Controls />
