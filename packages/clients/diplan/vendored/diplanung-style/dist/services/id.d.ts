/**
 * Returns a unique id. Can be used with prefix and suffix.
 * @param prefix
 * @param suffix
 * @returns {string}
 */
export declare function getUniqueId(prefix?: string, suffix?: string): string;
